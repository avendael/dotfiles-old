;;; avendael-ag.el --- Avendael's ag customizations

;;; Commentary:
;;
;; Not much really, but a convention is a convention.
;;
;;; Code:
(require 'ag)

(setq ag-reuse-buffers 't)

(provide 'avendael-ag)
;;; avendael-expand-region.el ends here
