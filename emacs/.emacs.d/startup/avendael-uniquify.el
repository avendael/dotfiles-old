;;; avendael-uniquify.el --- Avendael's uniquify customizations

;;; Commentary:
;;
;; Nothing much really, but a convention is a convention.
;;
;;; Code:
(require 'uniquify)

(setq uniquify-buffer-name-style 'forward)

(provide 'avendael-uniquify)
;;; avendael-uniquify.el ends here
