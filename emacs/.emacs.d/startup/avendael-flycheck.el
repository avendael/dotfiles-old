;;; avendael-flycheck.el --- Avendael's flycheck customizations

;;; Commentary:
;;
;; Nothing much really, but a convention is a convention.
;;
;;; Code:
(add-hook 'after-init-hook #'global-flycheck-mode)

(provide 'avendael-flycheck)
;;; avendael-flycheck.el ends here
