;;; avendael-secrets --- Avendael's top secret secrets

;;; Commentary:
;;
;; Not! Avendael's secrets aren't really here.  Sorry.
;;
;;; Code:
(load-library "secrets.el.gpg")
(provide 'avendael-secrets)
;;; avendael-secrets.el ends here
